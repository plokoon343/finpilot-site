Drop screenshots here with these exact names. The site shows them in 3D device
frames (tilted browser + a floating phone), with "coming soon" placeholders
until the files exist.

WEB (desktop browser, dark theme, ~1280–1440px wide, demo data — not real figures):
  dashboard.png   — Financial Dashboard (balance, income/expense, recent activity)
  budget.png      — Budget page (spending-by-category + budget cards)
  health.png      — Financial Health (charts + key metrics, ideally the new Reports tab)

MOBILE (a clean screenshot from the Android app, just the screen — no status bar clutter):
  mobile-home.png — the redesigned Home (balance hero + "Manage your money" cards)

Optional extra mobile shots (nice for later per-feature pages):
  mobile-budget.png, mobile-goals.png, mobile-wallet.png

Tips: PNG; consistent dark theme; portrait for phone, landscape for desktop.
Once the files are in place I redeploy and the real screens appear in 3D.
