Drop the 3 web-app screenshots here, with these exact names:

  dashboard.png   — the Financial Dashboard (balance, income/expense, recent activity)
  budget.png      — the Budget page (spending-by-category pie + budget cards)
  health.png      — the Financial Health page (income/expense chart + key metrics)

Guidance:
- PNG, captured from the web app in a desktop browser.
- One consistent theme (dark recommended to match the brand).
- ~1280–1440px wide; the app content only (browser chrome is optional — the
  site already draws a browser frame around each image).
- Use sample/demo data, not real personal figures.

Once these three files are in place, the Screens section renders them and the
site is ready to deploy.
